<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base activity theme â€” used as postSplashScreenTheme -->
    <style name="Theme.WSIW" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen theme â€” set on the activity in AndroidManifest.xml -->
    <style name="Theme.WSIW.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.WSIW</item>
    </style>

</resources>
