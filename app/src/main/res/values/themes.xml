<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base activity theme — used as postSplashScreenTheme -->
    <style name="Theme.WSIW" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/cinema_black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash screen theme — set on the activity in AndroidManifest.xml -->
    <style name="Theme.WSIW.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/cinema_black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.WSIW</item>
    </style>

</resources>
